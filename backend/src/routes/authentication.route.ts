// v1/authentication.route.ts
import { Router } from 'express';
const router = Router();

// router.get('/user/login', function (req: Request, res: Response, next){
    
// });
// router.use('/admin/login', adminLoginFunction);

export default router;

// v2/authentication.route.ts
// Similar structure with potentially different implementations
